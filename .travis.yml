language: scala

scala:
  - 2.12.4

cache:
  directories:
    - $HOME/.ivy2/cache
    - $HOME/.sbt

jdk:
   - oraclejdk8

dist: trusty

script:
  - sbt clean coverage test coverageReport

after_success:
  - bash <(curl -s https://codecov.io/bash)

notifications:
  email:
    on_success: change
    on_failure: always
