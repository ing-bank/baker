
baas.env = "test"
baas.env = ${?BAAS_ENV}

baas-component.baker-event-listener-port = "8081"
baas-component.baker-event-listener-port = ${?BAAS_BAKER_EVENT_LISTENER_PORT}

baas.state-node-hostname = "http://localhost:8085"
baas.state-node-hostname = ${?BAAS_STATE_NODE_HOSTNAME}

baas-component.http-api-port = "8080"
baas-component.http-api-port = ${?BAAS_COMPONENT_HTTP_API_PORT}

encryption.enabled = false
encryption.enabled = ${?BAAS_INGREDIENT_ENCRYPTION_ENABLED}

encryption.secret = ""
encryption.secret = ${?BAAS_INGREDIENT_ENCRYPTION_SECRET}
