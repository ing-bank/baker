akka {
  // prints warning if java serialization is used
  // this setting as off also turns on this config: enable-additional-serialization-bindings
  // not needed after akka 2.5 upgrade since java-serialization will be turned off by default.
  allow-java-serialization = off
}